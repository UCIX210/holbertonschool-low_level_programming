#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - random numbers, negative or positive
 *
 * Return: 0 Always
 */
int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		printf("%d", i);
	}
	return (0);
}
