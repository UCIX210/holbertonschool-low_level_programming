#include <stdio.h>
/**
 * main - entry point
 * @argc: counts arguments from command line
 * @argv: string to array of arguments
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
